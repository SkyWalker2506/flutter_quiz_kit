name: flutter_quiz_kit
description: "Quiz engine for Flutter — question generation, adaptive difficulty, sound feedback, STT matching, abuse detection."
version: 0.1.0
homepage: https://github.com/SkyWalker2506/flutter_quiz_kit

environment:
  sdk: ^3.11.3
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  audioplayers: ^6.1.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
